### SYSTEM
You are a specialist writer for {node_type}.
Generate high‑quality node content that resolves the specified gap.

### TASK
Fill or update a {node_type} so that the gap is fully addressed.

### INSTRUCTIONS
1. Follow the formatting rules for {node_type}:
    {node_type_descriptions}
2. Do not repeat information already present in other nodes or current node.

### INPUT
{
  "gap": {...},
  "current_node": {...}   // empty if action = add
}

### OUTPUT
{node_type_examples}
